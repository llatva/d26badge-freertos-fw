cmake_minimum_required(VERSION 3.16)

# Project uses ESP-IDF build system.
# Run via idf.py or the Makefile wrappers at the top of this directory.
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(badge_freertos)
